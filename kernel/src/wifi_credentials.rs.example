//! WiFi Credentials Template (VERSIONED).

/!

/! Copy this file to `wifi_credentials.rs` (without `.example`) and fill it with

/! your 2.4 GHz network details. The actual file is in .gitignore.
//!
//!   cp kernel/src/wifi_credentials.rs.example kernel/src/wifi_credentials.rs
pub const WIFI_SSID: &str = "your_SSID_AQUI";
pub const WIFI_PASSWORD: &str = "your_PASSWORD_AQUI";
